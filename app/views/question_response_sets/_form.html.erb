<%= form_for(question_response_set) do |f| %>
  <% if question_response_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_response_set.errors.count, "error") %> prohibited this question_response_set from being saved:</h2>

      <ul>
      <% question_response_set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %>
    <%= f.number_field :question_id %>
  </div>

  <div class="field">
    <%= f.label :response_set_id %>
    <%= f.number_field :response_set_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
